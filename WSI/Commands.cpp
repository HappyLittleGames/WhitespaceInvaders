#include "Commands.h"


void Commands::EnterCommand(sf::RenderWindow & window, const std::string& command)
{
	std::cout << "attempting to run commmand '" << command << "' for no reason" << std::endl;
}
